// Compat: os tipos de exibição da agenda foram para o módulo `modules/agenda`.
// Este arquivo é mantido só para não quebrar importadores antigos de
// "@/lib/agenda-data" (ex.: `lib/types` referencia `EventoTipo`).
export * from "@/modules/agenda/types";
